import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
  button: {
    width: '82%',
    marginVertical: 95,
    fontWeight: 'bold',
  },
});
export default styles;
