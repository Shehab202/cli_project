import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  image: {
    width: 100,
    height: 200,
    borderRadius: 8,
    marginLeft: 10,
    resizeMode: 'contain',
  },
  listStyle: {
    margin: 10,
    height: 150,
    borderRadius: 8,
    overflow: 'hidden',
    borderWidth: 1,
    borderColor: 'gray',
    padding: 18,
  
  },
});
export default styles;
